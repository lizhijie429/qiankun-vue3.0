const config = {
  // 微应用配置
  Sub01MicroApp: import.meta.env.VITE_MICRO_SUB01,
  Sub02MicroApp: import.meta.env.VITE_MICRO_SUB02,
  // 后台接口配置
  API_URL: import.meta.env.VITE_API_URL
}

export default config
